# MiniCPM-o 4.5 /v1/realtime full-duplex deploy on one large-memory GPU.
# MiniCPM-o is always run in full-duplex mode here -- there is no separate
# offline/benchmark variant of this config.
#
# Notable choices, relative to a naively-greedy offline-benchmark config:
#   * default_sampling_params on stage 0 (thinker) use the reference
#     streaming_generate's own duplex decode defaults (temperature=0.7,
#     top_p=0.8, top_k=20, text_repetition_penalty=1.05 -> repetition_penalty)
#     instead of greedy (temperature=0.0, top_k=-1) -- greedy decoding makes
#     a live conversational duplex session degenerate/repetitive.
#   * logits_processors registers MiniCPMOChunkEosForceLogitsProcessor,
#     which forces <|chunk_eos|> once a chunk's freely-sampled tokens hit
#     the reference's max_new_speak_tokens_per_chunk=20 cap; without it a
#     misbehaving generation could run past its ~1s chunk budget and never
#     yield back for the next audio chunk. See connection.py's
#     MINICPMO_MAX_NEW_TOKENS_PER_CHUNK / _run_streaming_session.
#   * stage 0's max_tokens is set high (32768), not the ~2048 a short-turn
#     benchmark config would use: this
#     SamplingParams field counts *all* generated tokens over the entire
#     persistent /v1/realtime streaming request's lifetime (potentially a
#     long-running conversation, not one short turn), so a small cap would
#     hit FINISHED_LENGTH_CAPPED and permanently end the session partway
#     through a real call. Set to a large, comfortably-below-context-window
#     value here; the model's own max_model_len is still the real ceiling
#     vLLM enforces underneath this.
pipeline: minicpmo_4_5
async_chunk: true

trust_remote_code: true
enable_prefix_caching: false

connectors:
  connector_of_shared_memory:
    name: SharedMemoryConnector
    extra:
      connector_get_sleep_s: 0.01
      connector_get_max_wait_first_chunk: 3000
      connector_get_max_wait: 300
      codec_chunk_frames: 25
      codec_left_context_frames: 3

stages:
  - stage_id: 0
    max_num_seqs: 4
    gpu_memory_utilization: 0.55
    enforce_eager: false
    tensor_parallel_size: 1
    max_num_batched_tokens: 16384
    devices: "0"
    logits_processors:
      - "vllm_omni.model_executor.models.minicpmo_4_5.chunk_eos_logits_processor:MiniCPMOChunkEosForceLogitsProcessor"
    limit_mm_per_prompt:
      # Daily-Omni MiniCPM pack: up to 64 interleaved image/audio pairs (1fps).
      image: 64
      audio: 64
      video: 1
    default_sampling_params:
      temperature: 0.7
      top_p: 0.8
      top_k: 20
      max_tokens: 32768
      seed: 42
      detokenize: true
      repetition_penalty: 1.05

  - stage_id: 1
    max_num_seqs: 4
    # Leave device-level headroom for the colocated HiFi-GAN cuDNN workspace.
    gpu_memory_utilization: 0.15
    enforce_eager: false
    max_num_batched_tokens: 8192
    devices: "0"
    input_connectors:
      from_stage_0: connector_of_shared_memory
    output_connectors:
      to_stage_2: connector_of_shared_memory
    # Codec sampling comes from the checkpoint's tts_config. These parameters
    # govern only vLLM's binary continue/stop token.
    default_sampling_params:
      temperature: 0.0
      top_p: 1.0
      top_k: -1
      min_tokens: 50
      max_tokens: 2048
      stop_token_ids: [1]
      seed: 42
      detokenize: false

  - stage_id: 2
    max_num_seqs: 4
    gpu_memory_utilization: 0.15
    enforce_eager: true
    enable_chunked_prefill: false
    max_num_batched_tokens: 65536
    max_model_len: 65536
    devices: "0"
    input_connectors:
      from_stage_1: connector_of_shared_memory
    default_sampling_params:
      temperature: 0.0
      top_p: 1.0
      top_k: -1
      max_tokens: 65536
      detokenize: true

platforms:
  cuda:
    stages:
      - stage_id: 1
        # Four Talker streams need much less KV than automatic sizing.
        # Leave single-GPU headroom for concurrent Stage 2 diffusion batches.
        kv_cache_memory_bytes: 2147483648  # 2 GiB

  npu:
    stages:
      - stage_id: 0
        max_num_batched_tokens: 8192
        compilation_config:
          cudagraph_mode: PIECEWISE
      - stage_id: 1
        max_num_batched_tokens: 8192
        compilation_config:
          cudagraph_mode: PIECEWISE
